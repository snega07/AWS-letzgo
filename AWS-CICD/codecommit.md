In enterprise -> people use private github repository. They install this on thier own server. Private gitlab, managed Git.

AWS code commit does not support root account.
Code commit -> Private Git repos. no public repos. Competitor to private repos used in enterprise and the servers.

Create Repo
Create IAM user -> policy codecommit power user, full access, read only

ssh, push, clone and other permission

Clone and work

Disadvantages:

Vendor specific, restricted to AWS alone.
less features -> no copilot, auto devops, cloud workspace.
UI is not great.

